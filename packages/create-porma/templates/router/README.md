# __PROJECT_NAME__

A routed Porma app created with `npm create porma@latest`.

```bash
npm install
npm run dev
```

Build for production:

```bash
npm run build
```

Routes are defined in `src/app/router.js`.
