<logic>
import AppNav from '../components/navigation/AppNav.blck'
</logic>

<view>
  <header class="app-shell">
    <div>
      <p class="eyebrow">Porma router app</p>
      <h1>{appTitle}</h1>
    </div>

    <AppNav />
  </header>
</view>

<style scoped>
.app-shell {
  align-items: center;
  display: flex;
  flex-wrap: wrap;
  gap: 1rem;
  justify-content: space-between;
  padding: 2rem 0 1rem;
}

.eyebrow {
  color: #64748b;
  font-size: 0.8rem;
  font-weight: 700;
  letter-spacing: 0.12em;
  margin: 0 0 0.25rem;
  text-transform: uppercase;
}

h1 {
  font-size: clamp(2rem, 8vw, 4rem);
  letter-spacing: -0.06em;
  margin: 0;
}
</style>
