# __PROJECT_NAME__

A Porma app created with `npm create porma@latest`.

```bash
npm install
npm run dev
```

Build for production:

```bash
npm run build
```

Porma components live in `src/` and use the `.blck` extension.
