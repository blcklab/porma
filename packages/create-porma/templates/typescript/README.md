# __PROJECT_NAME__

A TypeScript Porma app created with `npm create porma@latest`.

```bash
npm install
npm run dev
```

Build for production:

```bash
npm run build
```

Use `<logic lang="ts">` in `.blck` components.
